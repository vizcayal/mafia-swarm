# La Mafia — Forecasting Swarm
# pip install -r requirements.txt

# Core
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Modelos clásicos
statsforecast>=1.7       # AutoARIMA, AutoETS, AutoTheta

# Gradient boosting
lightgbm>=4.0
xgboost>=2.0
optuna>=3.0              # HPO

# Deep learning (opcional — requiere más memoria)
# neuralforecast>=1.7    # N-BEATS, PatchTST — descomentar cuando sea necesario

# Interpretabilidad
shap>=0.44

# Utilidades
tqdm>=4.0

# LLM backends (opcional — instalar el que vayas a usar)
# anthropic>=0.40             # Backend Claude API (MAFIA_BACKEND=api)
google-generativeai>=0.8    # Backend Google Gemini (MAFIA_BACKEND=gemini)
